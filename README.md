# Known Chrome Extension

Manifest V3 Chrome extension for finallyknown.ai. It auto-provisions a Known account, observes supported AI chat sites, ingests completed conversations, fetches memory context before sends, and exposes stats in the popup and action badge.

## Load locally

1. Open `chrome://extensions`
2. Enable Developer mode
3. Click `Load unpacked`
4. Select this folder
